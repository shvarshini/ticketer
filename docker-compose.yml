version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: ticketer
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ticketer
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
